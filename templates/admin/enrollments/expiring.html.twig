{% extends 'admin/base.html.twig' %}

{% block page_title %}Quote Iscrizione in Scadenza{% endblock %}
{% block page_subtitle %}Quote che scadranno nei prossimi 30 giorni{% endblock %}

{% block content %}
<div class="mb-6">
    <a href="{{ path('admin_enrollments') }}" class="inline-flex items-center text-primary-600 hover:text-primary-700 font-medium">
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
        </svg>
        Torna alle quote
    </a>
</div>

{% if enrollments|length > 0 %}
    <div class="bg-white rounded-lg shadow-md overflow-hidden">
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Cliente</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Palestra</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Importo</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Data Scadenza</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Giorni Rimanenti</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Azioni</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    {% for enrollment in enrollments %}
                        {% set daysRemaining = (enrollment.expiryDate.diff('now'|date_modify(''))).days %}
                        <tr class="hover:bg-gray-50 transition-colors">
                            <td class="px-6 py-4 whitespace-nowrap">
                                <div class="flex items-center">
                                    <div class="w-10 h-10 bg-primary-100 rounded-full flex items-center justify-center">
                                        <span class="text-primary-600 font-bold text-sm">
                                            {{ enrollment.user.firstName|slice(0, 1) ~ enrollment.user.lastName|slice(0, 1) }}
                                        </span>
                                    </div>
                                    <div class="ml-3">
                                        <p class="text-sm font-medium text-gray-900">
                                            {{ enrollment.user.firstName }} {{ enrollment.user.lastName }}
                                        </p>
                                        <p class="text-xs text-gray-500">{{ enrollment.user.email }}</p>
                                    </div>
                                </div>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <p class="text-sm text-gray-900">{{ enrollment.gym.name }}</p>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <p class="text-sm font-bold text-green-600">€{{ enrollment.amount }}</p>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <p class="text-sm text-gray-900">{{ enrollment.expiryDate|date('d/m/Y') }}</p>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                {% if daysRemaining <= 7 %}
                                    <span class="px-3 py-1 text-xs font-bold bg-red-100 text-red-800 rounded-full">
                                        {{ daysRemaining }} giorni
                                    </span>
                                {% elseif daysRemaining <= 14 %}
                                    <span class="px-3 py-1 text-xs font-bold bg-orange-100 text-orange-800 rounded-full">
                                        {{ daysRemaining }} giorni
                                    </span>
                                {% else %}
                                    <span class="px-3 py-1 text-xs font-bold bg-yellow-100 text-yellow-800 rounded-full">
                                        {{ daysRemaining }} giorni
                                    </span>
                                {% endif %}
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm space-x-2">
                                <a href="{{ path('admin_enrollment_user_history', {'userId': enrollment.user.id}) }}" class="text-blue-600 hover:text-blue-800 font-medium">
                                    Storico
                                </a>
                                <a href="{{ path('admin_enrollment_create', {'user_id': enrollment.user.id}) }}" class="text-green-600 hover:text-green-800 font-medium">
                                    Rinnova
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% else %}
    <div class="bg-white rounded-lg shadow-md p-12 text-center">
        <svg class="mx-auto h-16 w-16 text-green-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <p class="text-lg font-medium text-gray-900 mb-2">Nessuna quota in scadenza</p>
        <p class="text-sm text-gray-600">Tutte le quote iscrizione sono valide</p>
    </div>
{% endif %}
{% endblock %}