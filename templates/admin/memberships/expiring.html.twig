{% extends 'admin/base.html.twig' %}

{% block page_title %}Abbonamenti in Scadenza{% endblock %}
{% block page_subtitle %}Abbonamenti che scadono entro 30 giorni{% endblock %}

{% block content %}
{# Alert Banner #}
{% if memberships|length > 0 %}
<div class="mb-6 bg-orange-50 border-l-4 border-orange-400 rounded-lg shadow-md overflow-hidden">
    <div class="p-6">
        <div class="flex items-center">
            <svg class="w-8 h-8 text-orange-600 mr-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
            </svg>
            <div class="flex-1">
                <h3 class="text-lg font-bold text-orange-900 mb-1">{{ memberships|length }} {{ memberships|length == 1 ? 'Abbonamento in Scadenza' : 'Abbonamenti in Scadenza' }}</h3>
                <p class="text-orange-800">Questi abbonamenti richiedono attenzione e dovrebbero essere rinnovati a breve</p>
            </div>
        </div>
    </div>
</div>
{% endif %}

{# Memberships Table #}
<div class="bg-white rounded-lg shadow-md overflow-hidden">
    <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
                <tr>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        Utente
                    </th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        Palestra
                    </th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        Data Scadenza
                    </th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        Giorni Rimanenti
                    </th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        Piano
                    </th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        Azioni
                    </th>
                </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
                {% if memberships|length > 0 %}
                    {% for membership in memberships %}
                        {% set today = 'now'|date('Y-m-d') %}
                        {% set daysRemaining = membership.endDate.diff(date(today)).days %}
                        <tr class="hover:bg-gray-50 transition-colors">
                            <td class="px-6 py-4 whitespace-nowrap">
                                <div class="flex items-center">
                                    <div class="w-10 h-10 bg-primary-100 rounded-full flex items-center justify-center">
                                        <span class="text-primary-600 font-bold text-sm">
                                            {{ membership.user.firstName|slice(0, 1) ~ membership.user.lastName|slice(0, 1) }}
                                        </span>
                                    </div>
                                    <div class="ml-3">
                                        <p class="text-sm font-medium text-gray-900">
                                            {{ membership.user.firstName }} {{ membership.user.lastName }}
                                        </p>
                                        <p class="text-xs text-gray-500">
                                            {{ membership.user.email }}
                                        </p>
                                    </div>
                                </div>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <p class="text-sm text-gray-900">{{ membership.gym.name }}</p>
                                <p class="text-xs text-gray-500">{{ membership.gym.city }}</p>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <p class="text-sm font-medium text-gray-900">{{ membership.endDate|date('d/m/Y') }}</p>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="px-3 py-1 text-xs font-bold rounded-full
                                    {% if daysRemaining <= 7 %}bg-red-100 text-red-800
                                    {% elseif daysRemaining <= 14 %}bg-orange-100 text-orange-800
                                    {% else %}bg-yellow-100 text-yellow-800{% endif %}">
                                    {{ daysRemaining }} {{ daysRemaining == 1 ? 'giorno' : 'giorni' }}
                                </span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                {% if membership.subscriptionPlan %}
                                    <p class="text-sm text-gray-900">{{ membership.subscriptionPlan.name }}</p>
                                    <p class="text-xs text-gray-500">€{{ membership.actualPrice ?? membership.subscriptionPlan.price }}</p>
                                {% else %}
                                    <p class="text-sm text-gray-500">-</p>
                                {% endif %}
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm space-x-2">
                                <a
                                    href="{{ path('admin_membership_show', {'id': membership.id}) }}"
                                    class="inline-flex items-center px-3 py-1.5 bg-blue-600 text-white text-xs rounded-lg hover:bg-blue-700 transition-colors font-medium"
                                >
                                    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                                    </svg>
                                    Dettagli
                                </a>
                                <a
                                    href="{{ path('admin_membership_renew', {'id': membership.id}) }}"
                                    class="inline-flex items-center px-3 py-1.5 bg-green-600 text-white text-xs rounded-lg hover:bg-green-700 transition-colors font-medium"
                                >
                                    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                                    </svg>
                                    Rinnova
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                {% else %}
                    <tr>
                        <td colspan="6" class="px-6 py-12 text-center text-gray-500">
                            <svg class="mx-auto h-12 w-12 text-green-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <p class="text-lg font-medium text-gray-900 mb-2">Ottimo! Nessun abbonamento in scadenza</p>
                            <p class="text-sm mt-1">Tutti gli abbonamenti attivi hanno ancora almeno 30 giorni di validità</p>
                        </td>
                    </tr>
                {% endif %}
            </tbody>
        </table>
    </div>
</div>

{% if memberships|length > 0 %}
<div class="mt-6 bg-blue-50 border-l-4 border-blue-400 p-4 rounded-lg">
    <div class="flex">
        <svg class="w-5 h-5 text-blue-600 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <div class="text-sm text-blue-800">
            <p class="font-medium mb-1">Suggerimento</p>
            <p>Contatta questi clienti per rinnovare il loro abbonamento. Puoi offrire sconti o mesi bonus per fidelizzare i clienti di lunga data.</p>
        </div>
    </div>
</div>
{% endif %}
{% endblock %}