{% extends 'admin/base.html.twig' %}

{% block page_title %}Impostazioni{% endblock %}
{% block page_subtitle %}Configura le impostazioni della tua palestra{% endblock %}

{% block content %}
<div class="max-w-4xl">
    <form method="post" action="{{ path('admin_settings_update') }}" class="space-y-6">
        {# Orari Calendario Corsi #}
        <div class="bg-white rounded-lg shadow-md p-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-6">Calendario Corsi</h3>
            <p class="text-sm text-gray-600 mb-6">
                Configura gli orari di visualizzazione del calendario corsi. Queste impostazioni determinano
                quali fasce orarie vengono mostrate nel calendario settimanale.
            </p>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                <div>
                    <label for="course_schedule_start" class="block text-sm font-medium text-gray-700 mb-2">
                        Orario Inizio
                    </label>
                    <input
                        type="time"
                        id="course_schedule_start"
                        name="course_schedule_start"
                        value="{{ settings.courseScheduleStart|date('H:i') }}"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
                    >
                    <p class="text-xs text-gray-500 mt-1">Es. 07:00</p>
                </div>

                <div>
                    <label for="course_schedule_end" class="block text-sm font-medium text-gray-700 mb-2">
                        Orario Fine
                    </label>
                    <input
                        type="time"
                        id="course_schedule_end"
                        name="course_schedule_end"
                        value="{{ settings.courseScheduleEnd|date('H:i') }}"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
                    >
                    <p class="text-xs text-gray-500 mt-1">Es. 22:00</p>
                </div>

                <div>
                    <label for="time_slot_duration" class="block text-sm font-medium text-gray-700 mb-2">
                        Durata Slot (minuti)
                    </label>
                    <select
                        id="time_slot_duration"
                        name="time_slot_duration"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
                    >
                        <option value="30" {% if settings.timeSlotDuration == 30 %}selected{% endif %}>30 minuti</option>
                        <option value="60" {% if settings.timeSlotDuration == 60 %}selected{% endif %}>60 minuti</option>
                        <option value="90" {% if settings.timeSlotDuration == 90 %}selected{% endif %}>90 minuti</option>
                    </select>
                    <p class="text-xs text-gray-500 mt-1">Intervallo griglia calendario</p>
                </div>
            </div>

            <div class="mt-6 p-4 bg-blue-50 border border-blue-200 rounded-lg">
                <div class="flex">
                    <svg class="w-5 h-5 text-blue-600 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <div class="text-sm text-blue-800">
                        <p class="font-medium mb-1">Anteprima</p>
                        <p>
                            Il calendario mostrerà fasce orarie da
                            <strong>{{ settings.courseScheduleStart|date('H:i') }}</strong> a
                            <strong>{{ settings.courseScheduleEnd|date('H:i') }}</strong>
                            con intervalli di <strong>{{ settings.timeSlotDuration }} minuti</strong>.
                        </p>
                    </div>
                </div>
            </div>
        </div>

        {# Quota Iscrizione Globale #}
        <div class="bg-white rounded-lg shadow-md p-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-6">Quota Iscrizione</h3>
            <p class="text-sm text-gray-600 mb-6">
                Imposta la quota di iscrizione/affiliazione per la tua palestra. Questa quota verrà applicata automaticamente
                a tutti gli iscritti attuali e futuri.
            </p>

            <div class="flex items-start mb-6">
                <div class="flex items-center h-5">
                    <input
                        id="enable_enrollment_fee"
                        name="enable_enrollment_fee"
                        type="checkbox"
                        value="1"
                        {% if settings.isEnableEnrollmentFee %}checked{% endif %}
                        class="w-5 h-5 text-primary-600 border-gray-300 rounded focus:ring-primary-500"
                    >
                </div>
                <div class="ml-4">
                    <label for="enable_enrollment_fee" class="font-medium text-gray-900">
                        Abilita Quota Iscrizione
                    </label>
                    <p class="text-sm text-gray-500 mt-1">
                        Quando attiva, verrà richiesta la quota di iscrizione per tutti i nuovi iscritti.
                    </p>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                    <label for="enrollment_fee_amount" class="block text-sm font-medium text-gray-700 mb-2">
                        Importo Quota (€) *
                    </label>
                    <input
                        type="number"
                        id="enrollment_fee_amount"
                        name="enrollment_fee_amount"
                        step="0.01"
                        min="0"
                        value="{{ gym.enrollmentFeeAmount }}"
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
                        placeholder="25.00"
                    >
                    <p class="text-xs text-gray-500 mt-1">Importo della quota iscrizione annuale</p>
                </div>

                <div>
                    <label for="enrollment_fee_validity_months" class="block text-sm font-medium text-gray-700 mb-2">
                        Validità (mesi)
                    </label>
                    <input
                        type="number"
                        id="enrollment_fee_validity_months"
                        name="enrollment_fee_validity_months"
                        min="1"
                        max="24"
                        value="{{ gym.enrollmentFeeValidityMonths ?? 12 }}"
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
                    >
                    <p class="text-xs text-gray-500 mt-1">Durata validità quota (default 12 mesi)</p>
                </div>
            </div>

            <div class="mt-6 p-4 bg-blue-50 border border-blue-200 rounded-lg">
                <div class="flex">
                    <svg class="w-5 h-5 text-blue-600 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <div class="text-sm text-blue-800">
                        <p class="font-medium mb-1">Come funziona</p>
                        <p>
                            Imposta la quota di <strong>€{{ gym.enrollmentFeeAmount ?? '0.00' }}</strong> che tutti gli iscritti devono pagare.
                            Nel dettaglio di ogni utente vedrai se ha pagato o meno la quota, e potrai registrare il pagamento quando lo ricevi.
                        </p>
                    </div>
                </div>
            </div>
        </div>

        {# Check-in Tornello #}
        <div class="bg-white rounded-lg shadow-md p-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-6">Check-in Ingresso Palestra</h3>
            <p class="text-sm text-gray-600 mb-6">
                Abilita il sistema di check-in tornello per l'ingresso in palestra.
                Gli iscritti dovranno timbrare l'ingresso e verranno verificati automaticamente abbonamento e certificato medico.
            </p>

            <div class="flex items-start">
                <div class="flex items-center h-5">
                    <input
                        id="enable_gym_check_in"
                        name="enable_gym_check_in"
                        type="checkbox"
                        value="1"
                        {% if settings.isEnableGymCheckIn %}checked{% endif %}
                        class="w-5 h-5 text-primary-600 border-gray-300 rounded focus:ring-primary-500"
                    >
                </div>
                <div class="ml-4">
                    <label for="enable_gym_check_in" class="font-medium text-gray-900">
                        Abilita Check-in Tornello
                    </label>
                    <p class="text-sm text-gray-500 mt-1">
                        Quando attivo, gli utenti potranno timbrare l'ingresso in palestra tramite tornello fisico o app.
                        Il sistema verificherà automaticamente:
                    </p>
                    <ul class="text-sm text-gray-500 mt-2 ml-4 space-y-1">
                        <li class="flex items-center">
                            <svg class="w-4 h-4 text-green-500 mr-2" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                            </svg>
                            Abbonamento attivo e non scaduto
                        </li>
                        <li class="flex items-center">
                            <svg class="w-4 h-4 text-green-500 mr-2" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                            </svg>
                            Certificato medico valido e non scaduto
                        </li>
                    </ul>
                </div>
            </div>

            {% if settings.isEnableGymCheckIn %}
                <div class="mt-6 p-4 bg-green-50 border border-green-200 rounded-lg">
                    <div class="flex">
                        <svg class="w-5 h-5 text-green-600 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <div class="text-sm text-green-800">
                            <p class="font-medium">Check-in tornello attivo</p>
                            <p class="mt-1">Gli iscritti possono ora timbrare l'ingresso in palestra.</p>
                        </div>
                    </div>
                </div>
            {% endif %}
        </div>

        {# Actions #}
        <div class="flex gap-4">
            <button
                type="submit"
                class="px-6 py-3 bg-primary-600 text-white rounded-lg hover:bg-primary-700 transition-colors font-medium"
            >
                <svg class="w-5 h-5 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                Salva Impostazioni
            </button>

            <a
                href="{{ path('admin_dashboard') }}"
                class="px-6 py-3 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition-colors font-medium"
            >
                Annulla
            </a>
        </div>
    </form>
</div>
{% endblock %}
