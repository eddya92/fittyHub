<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            color: white;
            padding: 30px;
            text-align: center;
            border-radius: 10px 10px 0 0;
        }
        .content {
            background: #f9f9f9;
            padding: 30px;
            border-radius: 0 0 10px 10px;
        }
        .button {
            display: inline-block;
            padding: 15px 30px;
            margin: 20px 10px;
            background: #f5576c;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }
        .button.decline {
            background: #95a5a6;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            font-size: 12px;
            color: #999;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🏢 Invito Collaborazione Palestra</h1>
    </div>
    <div class="content">
        <p>Ciao {{ trainerName }},</p>

        <p>La palestra <strong>{{ gymName }}</strong> ti ha invitato a collaborare come Personal Trainer!</p>

        <p><strong>Dettagli palestra:</strong></p>
        <ul>
            <li>📍 {{ gymAddress }}, {{ gymCity }}</li>
            {% if gymPhone %}<li>📞 {{ gymPhone }}</li>{% endif %}
            {% if gymEmail %}<li>✉️ {{ gymEmail }}</li>{% endif %}
        </ul>

        <p><strong>Cosa puoi fare?</strong></p>
        <ul>
            <li>Gestire i tuoi clienti della palestra</li>
            <li>Creare schede di allenamento personalizzate</li>
            <li>Monitorare progressi e presenze</li>
            <li>Comunicare con i tuoi atleti</li>
        </ul>

        {% if message %}
        <p><strong>Messaggio dalla palestra:</strong><br>
        <em>"{{ message }}"</em></p>
        {% endif %}

        <div style="text-align: center; margin: 30px 0;">
            <a href="{{ acceptUrl }}" class="button">✅ Accetta Collaborazione</a>
            <a href="{{ declineUrl }}" class="button decline">❌ Rifiuta</a>
        </div>

        <p><small>Questo invito scadrà il {{ expiresAt|date('d/m/Y H:i') }}</small></p>
    </div>
    <div class="footer">
        <p>FITTY HUB - Piattaforma SaaS per professionisti del fitness</p>
        <p>Se non conosci questa palestra, puoi ignorare questa email</p>
    </div>
</body>
</html>
