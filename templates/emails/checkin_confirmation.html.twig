<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #00BCD4; color: white; padding: 20px; text-align: center; border-radius: 5px; }
        .content { background: #f9f9f9; padding: 20px; margin-top: 20px; border-radius: 5px; }
        .success { background: #B2EBF2; padding: 20px; margin: 15px 0; border-radius: 5px; text-align: center; }
        .details { background: white; padding: 15px; margin: 15px 0; border-left: 4px solid #00BCD4; }
        .footer { text-align: center; margin-top: 20px; font-size: 12px; color: #666; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>✅ Check-in Effettuato!</h1>
        </div>

        <div class="content">
            <p>Ciao {{ user.firstName }},</p>

            <div class="success">
                <h2 style="margin: 0; color: #00838F;">Benvenuto in palestra!</h2>
            </div>

            <div class="details">
                <h3>🏋️ Dettagli Check-in</h3>
                <p><strong>Palestra:</strong> {{ gym.name }}</p>
                <p><strong>Data e Ora:</strong> {{ attendance.checkInTime|date('d/m/Y H:i') }}</p>
                <p><strong>Tipo:</strong> {{ attendance.type == 'gym_entrance' ? 'Ingresso Palestra' : 'Sessione Corso' }}</p>
            </div>

            <p>Buon allenamento! 💪</p>

            <p style="background: #FFF9C4; padding: 15px; border-radius: 5px; font-size: 14px;">
                💡 <strong>Consiglio del giorno:</strong> Ricordati di idratarti durante l'allenamento e fai sempre stretching prima e dopo.
            </p>
        </div>

        <div class="footer">
            <p>FittyHub - Il tuo partner per il fitness</p>
        </div>
    </div>
</body>
</html>
