<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #673AB7; color: white; padding: 20px; text-align: center; border-radius: 5px; }
        .content { background: #f9f9f9; padding: 20px; margin-top: 20px; border-radius: 5px; }
        .highlight { background: #E1BEE7; padding: 20px; margin: 15px 0; border-radius: 5px; text-align: center; }
        .details { background: white; padding: 15px; margin: 15px 0; border-left: 4px solid #673AB7; }
        .footer { text-align: center; margin-top: 20px; font-size: 12px; color: #666; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🏋️ Nuovo Piano Allenamento</h1>
        </div>

        <div class="content">
            <p>Ciao {{ user.firstName }},</p>

            <div class="highlight">
                <h2 style="margin: 0; color: #673AB7;">Il tuo Personal Trainer ti ha assegnato un nuovo piano!</h2>
            </div>

            <div class="details">
                <h3>📋 Dettagli Piano</h3>
                <p><strong>Nome:</strong> {{ plan.name }}</p>
                {% if plan.description %}
                <p><strong>Descrizione:</strong> {{ plan.description }}</p>
                {% endif %}
                <p><strong>Obiettivo:</strong> {{ plan.goal|default('Non specificato') }}</p>
                <p><strong>Durata:</strong> {{ plan.weeksCount }} settimane</p>
                <p><strong>Data Inizio:</strong> {{ plan.startDate|date('d/m/Y') }}</p>
                <p><strong>Data Fine:</strong> {{ plan.endDate|date('d/m/Y') }}</p>
                <p><strong>Personal Trainer:</strong> {{ trainer.user.firstName }} {{ trainer.user.lastName }}</p>
            </div>

            <p>Accedi all'app per visualizzare il piano completo e iniziare il tuo percorso!</p>

            <p style="background: #E8F5E9; padding: 15px; border-radius: 5px;">
                💪 <strong>Ricorda:</strong> La costanza è la chiave del successo. Segui il piano e vedrai i risultati!
            </p>
        </div>

        <div class="footer">
            <p>FittyHub - Il tuo partner per il fitness</p>
        </div>
    </div>
</body>
</html>
